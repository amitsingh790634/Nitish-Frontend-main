import 'package:astro_app/utils/packag.dart';

class SettingController extends GetxController {
  RxBool isActive = false.obs;

  // void toggle({required RxBool on}) => on.value = on.value ? true : false;
}
